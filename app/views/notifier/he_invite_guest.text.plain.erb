שלום <%= h(@guest.name)%>,

קיבלת הזמנה מ<%= h(@guest.event.user.name) %> ל <%= "\"#{h(@guest.event.name)}\"" %>.

לפתיחת ההזמנה יש ללחוץ על הקישור הבא:
<%= @url %>

אם לחיצה על הקישור אינה פותחת את ההזמנה, ניתן להעתיק את שורת הקישור ולהדביק אותה בשורת הכתובת בדפדפן.

אין צורך להשיב למייל זה.

בברכה,
צוות Eventify
