<%= @guest.name%> היקר,

הינך מוזמן לאירוע: <%= "\"#{h(@guest.event.name)}\"" %> של <%= h(@guest.event.user.name) %>.

אנא לחץ על קישור זה על מנת לפתוח את ההזמנה ולהגיב עליה 
<%= @url %>

אם לחיצה על הקישור אינה פותחת את ההזמנה, העתק את שורת הקישור והדבק אותה בשורת הכתובת בדפדפן בו אתה משתמש.

בברכה,
צוות Eventify.
