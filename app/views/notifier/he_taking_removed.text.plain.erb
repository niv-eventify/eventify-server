שלום <%= h(@guest.name)%>,

אין צורך בהבאת <%= h(@thing.name)%> ל<%= "\"#{h(@guest.event.name)}\"" %> של <%= h(@guest.event.user.name)%>.

ניתן לבחור פריטים אחרים מרשימת ה"דברים להביא" ע"י לחיצה על הקישור הבא:
<%= @url %>

אין צורך להשיב למייל זה.

בברכה,
צוות Eventify