%h1 New category

- semantic_form_for(@category, :live_validations => true) do |f|
  = f.object.errors.on(:base)
  != render :partial => "form", :locals => { :f => f }
  != f.submit "Create"
  != link_to 'Back', categories_path 