- if design
  .img-box-t
    .img-box-b
      .content-h
        .image
          %a{ :href => design.preview.url(:stage2), :id => "light_box_#{design.id}", :class => "lightbox" }
            != image_tag(design.preview_with_text.url(:medium), :alt => "")
      != select_design_link(category, design)
      != select_design_link_plus(design)

