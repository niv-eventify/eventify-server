%tr[guest]

  - info_table_column(1) do
    %input#name-1.input-check{ :type => "checkbox" }
    %label{ :for => "name-1" }= guest.name

  - info_table_column(2) do
    != mail_to guest.email, guest.email

  - info_table_column(3) do
    = guest.mobile_phone

  - info_table_column(4) do
    - if_not_blank_editable_property(:send_email, :email, @event, guest)

  - info_table_column(5) do
    - if_not_blank_editable_property(:send_sms, :mobile_phone, @event, guest)

  - info_table_column(6) do
    - if_not_blank_editable_property(:allow_snow_ball, :mobile_phone, @event, guest)

  - info_table_column do
    != link_to_remove_guest(@event, guest)
