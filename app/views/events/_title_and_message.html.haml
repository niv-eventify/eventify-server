- if event.design.is_seperated_title? 
  .title-holder{:style => stage2_title_css(event.design)}
    #title.title
      != event.name
.msg-holder{:style => stage2_message_css(event.design)}
  - if !event.design.is_seperated_title? 
    #title.title{:style => stage2_title_css(event.design)}
      != event.name
  #free_text.free_text
    != event.guest_message