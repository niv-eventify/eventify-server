require 'spec_helper'

describe UploadedMap do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    UploadedMap.create!(@valid_attributes)
  end
end
