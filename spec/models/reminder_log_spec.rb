require 'spec_helper'

describe ReminderLog do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    ReminderLog.create!(@valid_attributes)
  end
end
